% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pyramid.R
\name{plot_pyramid}
\alias{plot_pyramid}
\title{Draw a Population Pyramid}
\usage{
plot_pyramid(data, colors = c("steelblue", "orange"))
}
\arguments{
\item{data}{A tibble or dataframe. Expected to have Age in the first column,
followed by male and female population columns (e.g., "Male", "Female").}

\item{colors}{A character vector of length 2. The first color corresponds to males,
and the second to females. Defaults to c("steelblue", "orange").}
}
\value{
A ggplot object representing the population pyramid.
}
\description{
Generates a population pyramid plot for demographic data (like \code{pop7str}).
It transforms the data into a format suitable for \code{ggplot2},
negates male population values for symmetric visualization,
and applies a customizable color scheme.
}
\examples{
data(pop7str)
plot_pyramid(pop7str)
plot_pyramid(pop7str, colors = c("blue", "red"))

}
